{
  "name": "ssh-admin",
  "version": "1.0.0",
  "description": "MCP server for SSH remote server management",
  "bin": {
    "ssh-admin": "./dist/bundle.cjs"
  },
  "scripts": {
    "build": "tsc && node build.mjs",
    "bundle": "node build.mjs",
    "start": "node dist/bundle.cjs",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "mcp",
    "ssh",
    "server"
  ],
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ssh2": "^1.17.0",
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.2",
    "@types/ssh2": "^1.15.5",
    "@vitest/coverage-v8": "^4.1.10",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.8",
    "vitest": "^4.1.10"
  }
}
